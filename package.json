{
  "name": "emotion-checkin",
  "version": "0.1.0",
  "private": true,
  "description": "1日数回の絵文字感情記録 + 週次保護者通知 (Chrome Extension, Manifest V3)",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "lint": "tsc --noEmit",
    "package": "npm run build && node scripts/package.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
